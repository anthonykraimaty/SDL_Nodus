# Database (Docker PostgreSQL - Internal Network)
DATABASE_URL="postgresql://nodus_user:nodus_password@postgres:5432/nodus_db?schema=public"

# Server
PORT=3001
NODE_ENV=production

# CORS - Update this with your production frontend URL
CLIENT_URL=http://localhost:5173

# JWT Authentication - CHANGE THESE IN PRODUCTION
JWT_SECRET=change-this-to-a-very-secure-random-string-in-production
JWT_EXPIRES_IN=7d
